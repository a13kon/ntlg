services:
  storage:
    image: redis
    volumes: 
      - ./data:/data
  counter:
    image: a13kon/counter:v2.0.0
    ports:
      - 80:3002
    environment:
      - PORT=3002
      - REDIS_URL=redis://storage
    depends_on:
      - storage